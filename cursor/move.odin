package cursor 

// Cursor movement

move_to :: proc(row: int, col: int) -> (ok: bool) {}

move_up :: proc(n: int) -> (ok: bool) {}

move_down :: proc(n: int) -> (ok: bool) {}

move_right :: proc(n: int) -> (ok: bool) {}

move_left :: proc(n: int) -> (ok: bool) {}